# DB credentials + JWT_SECRET come from the root db.env (shared with the other back-ends).

# --- Century Game player API (same signing as autoredeemgifts) ---
REDEEM_SECRET=changeme
# REDEEM_API=https://kingshot-giftcode.centurygame.com/api

# --- HTTP API ---
# ALLOWED_ORIGINS=http://localhost:5173

# --- OCR & name matching (optional tuning) ---
# OCR_MIN_CONFIDENCE=0.50
# OCR_MIN_NAME_LENGTH=2
# OCR_MAX_NAME_LENGTH=30
# OCR_IGNORE_WORDS=rank,score,points,power,total,damage,kills,victory,defeat,legion,results
# STRIP_ALLIANCE_TAG=true
# FUZZY_MATCH_THRESHOLD=85
# OCR_MIN_POWER_VALUE=1000
# OCR_ROW_Y_TOLERANCE_RATIO=0.6
