# DB credentials + JWT_SECRET come from the root db.env (shared with the
# other back-ends).

# --- JWT (optional tuning, JWT_SECRET itself lives in root db.env) ---
# JWT_TTL_HOURS=8

# --- HTTP API ---
# ALLOWED_ORIGINS=http://localhost:5173
