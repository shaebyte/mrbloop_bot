# Secrets & env files
.env
.env.*
db.env

# Frontend build-time config is not a secret (just the API base URL) and
# must be committed so `vite build` produces a working production bundle.
!mrbloop_FE/.env.production

# Python
__pycache__/
*.pyc
*.pyo
*.db

# Logs
*.log

# Node
node_modules/
dist/

# Claude
CLAUDE.md
CMD_COMMANDS.txt
CMD.txt
